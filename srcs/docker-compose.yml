version: '3'

services:
  nginx:
    image: nginx
    build: ./requirements/nginx
    container_name: nginx
    depends_on:
      - wordpress
    restart: on-failure
    networks:
      - inception
    ports:
      - 443:443
    volumes:
      - wordpress_volume:/var/www/wordpress

  mariadb:
    image:  mariadb
    build:  ./requirements/mariadb
    container_name: mariadb
    restart: on-failure
    networks:
      - inception
    volumes:
      - db_volume:/var/lib/mysql

  wordpress:
    image:      wordpress
    build:      ./requirements/wordpress
    container_name: wordpress
    depends_on:
      - mariadb
    restart: on-failure
    networks:
      - inception
    env_file:
      - ./.env
    volumes:
      - wordpress_volume:/var/www/wordpress

volumes:
  db_volume:
    name: "db_volume"
    driver_opts:
      type: none
      o: bind
      device: /home/ljahn/data/db_volume
  wordpress_volume:
    name: "wordpress_volume"
    driver_opts:
      type: none
      o: bind
      device: /home/ljahn/data/wordpress_volume

networks:
  inception:
    name: inception
    driver: bridge
