
[client-server]                                                                                        
socket=/var/lib/mysql/mysql.sock # for some reason it needs a connection to a unix socket!
port=3306

[mysqld]
bind-address=0.0.0.0
skip-networking = false
datadir=/var/lib/mysql